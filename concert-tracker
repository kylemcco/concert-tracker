#!/usr/bin/env ruby

if ARGV.length < 1
  print "[Help] Run as: ./concert-tracker manage"
elsif ARGV.length == 1
  if ARGV[0] != 'manage'
    print "[Help] Run as: ./concert-tracker manage"
  else
    print <<-EOS
1. Add a concert
2. Manage concerts
3. View statistics
4. Exit
EOS
  end
end
